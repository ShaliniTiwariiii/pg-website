export default function Footer(){
  return (
    <footer className="site-footer" id="contact">
      <div>© {new Date().getFullYear()} PGFinder — Find comfortable, secure PGs nearby.</div>
    </footer>
  );
}
